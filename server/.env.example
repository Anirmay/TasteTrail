# Copy this file to `.env` and fill in real values before running the server
# MongoDB connection URI (required)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/tastetrail?retryWrites=true&w=majority

# JWT secret used to sign tokens (required)
JWT_SECRET=your_jwt_secret_here

# Port the server will listen on (optional)
PORT=5000

# Optional: set a default admin email/password used by seed or local dev
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=changeme

# Notes:
# - Replace the placeholders above with real values. For local development you can run a local MongoDB
#   and use a connection string like: mongodb://localhost:27017/tastetrail
# - Never commit your real `.env` with secrets to version control.
# - After creating `.env`, run the server with `npm start` inside the `server` folder.
